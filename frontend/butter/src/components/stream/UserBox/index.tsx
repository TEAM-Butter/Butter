import styled from "@emotion/styled";

const UserBoxWrapper = styled.div`
  // styles here
`;

const UserBox = () => {
  return <UserBoxWrapper>// content here</UserBoxWrapper>;
};

export default UserBox;
