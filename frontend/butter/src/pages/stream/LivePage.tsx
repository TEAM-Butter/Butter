import styled from "@emotion/styled";

const LivePageWrapper = styled.div`
  // styles here
`;

const LivePage = () => {
  return <LivePageWrapper>LivePage</LivePageWrapper>;
};

export default LivePage;
