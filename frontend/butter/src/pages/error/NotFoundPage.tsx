import styled from "@emotion/styled";

const NotFoundPageWrapper = styled.div`
  // styles here
`;

const NotFoundPage = () => {
  return <NotFoundPageWrapper>Not Found</NotFoundPageWrapper>;
};

export default NotFoundPage;
